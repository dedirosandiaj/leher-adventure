[phases.setup]
nixPkgs = ["nodejs_20", "python3", "postgresql"]

[phases.build]
cmds = [
  "mkdir -p public/uploads/hero public/uploads/gallery public/uploads/team",
  "npm run build"
]

[start]
cmd = "chmod +x start.sh && ./start.sh"

[env]
NODE_ENV = "production"
PORT = "3000"
