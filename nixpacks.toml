[phases.build]
cmds = [
  "mkdir -p public/uploads/hero public/uploads/gallery public/uploads/team",
  "npm run build"
]

[phases.setup]
nixPkgs = ["nodejs_20", "python3"]

[start]
cmd = "npm start"

[env]
NODE_ENV = "production"
PORT = "3000"
